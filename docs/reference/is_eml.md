# Check if metadata is eml object

Helper function to validate that an argument belongs to the `emld`
class.

## Usage

``` r
is_eml(metadata)
```

## Arguments

- metadata:

  The metadata object returned by `load_metadata`. If parameter not
  provided, defaults to calling `load_metadata` in current project
  directory.
